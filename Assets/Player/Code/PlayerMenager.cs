using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class PlayerMenager : MonoBehaviour
{
    public int novelos;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       //TextMeshProUGUI novelo = transform.Find("CanvasPlayer").transform.Find("Novelo").GetComponent<TextMeshProUGUI>();
        //novelo.text = "X : " + novelos.ToString();
    }
    
}
